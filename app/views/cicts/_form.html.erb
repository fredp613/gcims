<%= simple_form_for(@cict) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :commitmentitem, label_method: :ci_name %>
    <%= f.association :customtemplate %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
