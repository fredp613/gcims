<%= simple_form_for(@customtemplate) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :commitmentitem %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
