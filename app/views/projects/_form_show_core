<fieldset>
<div class='content'>
	 <legend class='proj_legend'>Project Details</legend>

	 	<%= simple_form_for(@project) do |f| %>		  
		  <%= render 'project_fields', :f=>f %>
	  <% end %>
</div>

</fieldset>