<fieldset>
<div class='content'>
	 <legend class='proj_legend'>Project Details</legend>

	 	<%= simple_form_for(@project) do |f| %>		  
		  <%= render 'project_fields', :f=>f %>

		  <div class="submit">
    		<%= f.button :submit, "Save", :class=>'btn btn-small btn-info' %>
 		  </div>	
	  <% end %>
</div>
</fieldset>

