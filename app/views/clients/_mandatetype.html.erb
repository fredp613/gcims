
<% if @client.new_record?  %>
   Client Type: <b><%= Clienttype.where(:id=>@client.clienttype_id).first.name %></b>
     <%= f.button :submit, "change", :name=>'clienttype', :class=>'btn-small' %>
     <%= f.input :clienttype_id, as: :hidden %>
     
<% else %>
	
	    <%= link_to "change", new_client_clienttypes_path(:client_id=>@client.id),:class=>'btn-small' %><br>
	    
	<% if @clienttype != 1 %>
	 <%= f.input :mandate, :label=>'Mandate', :input_html=>{ rows: '5' } %>
	<% end %>

<% end  %>

