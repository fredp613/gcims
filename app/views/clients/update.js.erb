<% if @client.errors.any? %>
	 console.log('Error');

   
   if ($('#corporation_fields').is(':visible')) {
	   $('#corporation_fields').html('<%= j render ("clients/show_sections/corporation_fields") %>')
   }
   

   if ($('#charity_fields').is(':visible')) {
    $('#charity_fields').html('<%= j render ("clients/show_sections/charity_fields") %>')
   }
   

   if ($('#band_fields').is(':visible')) {
    $('#band_fields').html('<%= j render ("clients/show_sections/band_fields") %>')
   }

   if ($('#frmContainer')) {
     $('.loader').fadeOut(200);
     $('#frmContainer').html('<%= j render ('form') %>')
     $('#frmClient').formToWizard({ submitButton: 'clientSubmit' });
   }

	 //$('#frmClient').formToWizard({ submitButton: 'clientSubmit' });
<% else %>	
  	console.log('updated');
  	
  	 <% if params[:client][:corporation_attributes] %>
  	 $('#corp').html('<%= j render ("clients/show_sections/corp_fields") %>')
  	 <% end %>

  	 <% if params[:client][:charity_attributes] %>
  	 $('#charity').html('<%= j render ("clients/show_sections/ch_fields") %>')
  	 <% end %>

     <% if params[:client][:band_attributes] %>
     $('#band').html('<%= j render ("clients/show_sections/ba_fields") %>')
     <% end %>

<% end  %>