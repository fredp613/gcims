<%= form_for quicksearch_path, :method => 'get', :html => { :id=>'searchForm', :class => 'form-inline' } do |f|  %>
	<div class='input-append'>
	<%= f.text_field "Search", placeholder: 'Search anything'%>  
	<%= button_tag(:type=>:submit, :class=>'btn submit', :style=>'margin-bottom:5px') do %>
	  <i class='icon-search'></i>
	<% end %>
<% end %>