<%= simple_form_for(@clienttype) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>
 


	  <div class="form-actions">
	    <%= f.button :submit, :label=>"Save", :class=>'btn btn-primary btn-small' %>
	  </div>
 
<% end %>
